commands:
  install_nest:
    command: npm install -g @nestjs/cli
  install_mongocli:
    command: |
      yum erase $(sudo rpm -qa | grep mongodb-org)
      yum install -y mongodb-org
      service mongod start
      chkconfig mongod on
